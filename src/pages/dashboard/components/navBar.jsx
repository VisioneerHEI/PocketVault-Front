import React from "react";
import { Flex } from "@chakra-ui/react";

const NavBar = () => {
    return (
        <Flex
            h="70px">
                NavBar
        </Flex>
    )
};

export default NavBar;